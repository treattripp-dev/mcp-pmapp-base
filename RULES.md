# Project Rules

1. **Command Logging**: Every time a terminal command is executed by the agent, it MUST be added to `commands.md` if it is not already listed.
   - The log should include the command itself and a brief description.
